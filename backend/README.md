# Backend Django

Scaffold inicial do backend em Django + Django REST Framework.

Como usar (dev):

1. Criar e ativar virtualenv

```bash
python -m venv env
env\Scripts\activate
pip install -r requirements.txt
```

2. Rodar migrações e iniciar servidor

```bash
python manage.py migrate
python manage.py runserver
```
